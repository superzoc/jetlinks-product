# <任务名称>

## 目标

这次交付要解决什么。

## 非目标

这次明确不做什么。

## 当前状态

相关页面、路由、组件、数据源和已知问题。

## 文件范围

<!-- 多栈仓库（后端 Java + 前端 Vue 等）按栈分别列出；单栈仓库合并即可。 -->

允许修改：

后端：
- `<backend-modules>/<feature>/...`

前端：
- `<frontend>/<feature>-ui/...`

文档：
- `docs/tasks/<task>/...`
- `docs/requirements/<file>.md`（仅在收口阶段回写）

不建议修改：

- 不相关的既有模块（按需补充具体路径，例如其他 manager / 共享内核 / git submodule）

## 读取要求

<!-- 仅列入本任务实际相关的 must-reads；没有的层级删行（如仓库无 docs/constitution/）。 -->

开始前必须读取：

- `AGENTS.md` 或 `CLAUDE.md`（按你仓库的入口约定）
- `docs/tasks/<task>/handoff.md`
- `docs/constitution/...`（如存在）
- `docs/requirements/<相关文档>.md`
- 相关模块 README + 相邻参照模块（多栈仓库通常含一个后端 + 一个前端参照）

## 协作阶段

当前阶段：需求澄清 / UI 评审 / 工程实施 / 验证收口 / 已完成

当前执行者：Claude / Codex / Gemini / 人类开发者

下一步需要的输出物：

- 

## 交付标准

- 

## 风险

- 
